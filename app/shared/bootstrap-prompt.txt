请现在连接“康米巨星猎头系统”的 MCP，完成一次最小身份验证，然后立即告诉我连接结果。我已授权你使用下面的地址和专用凭据完成本次连接。

服务地址：{{MCP_SERVER_URL}}
认证配置：
{{AUTH_CONFIGURATION}}
凭据到期：{{CREDENTIAL_EXPIRES}}

执行顺序：
1. 如果当前已有可调用的对应 MCP 工具，直接调用 whoami；否则使用现有 HTTP 或终端能力连接上述地址。未在客户端配置中找到服务器，不等于无法连接；不要先扫描本机配置、环境变量、插件或搜索客户端文档，也不要先询问临时或持久化范围。
2. HTTP 接入使用上面的 Authorization，以及 Content-Type: application/json、Accept: application/json, text/event-stream。按 Streamable HTTP 完成 initialize、notifications/initialized，再 tools/call 调用 whoami（arguments 为 {}）；使用协商的协议版本及服务端返回的会话标识。可复用已有 MCP 客户端库，但不为一次验证先安装整套工具或启动另一位 Agent。
3. whoami 成功且服务、环境、成员身份符合预期，就立即简短报告“已连接”及身份。仅连接时到此完成，不枚举业务档案、不通读全部指南、不做额外读写测试；如果我同时给了业务任务，再按需读取对应指南并继续任务。
4. 只有实际连接失败才针对该错误排查。请求设置合理超时，暂时性网络错误最多重试一次；超过约 30 秒仍未连通，先说明正在尝试的步骤和具体阻塞，不静默检查数分钟。若客户端没有 HTTP/终端能力，或平台权限明确阻止请求，立即说明缺少的能力或权限，不宣称已经连接，不绕过限制。

默认先完成当前会话连接，持久化不是前置步骤。仅当我明确要求以后新会话可用时，才在私有客户端配置中保存，并保护已有设置；已明确客户端和范围就直接按要求执行，不重复确认。只有执行了持久化才报告保存位置和新会话验证情况，未验证就如实说明；普通连接结果不附带配置巡检清单。

无需打开浏览器、网页登录或再次申请凭据。不要在回复、日志或公开仓库中回显认证值；本次 HTTP 使用无需把凭据写入文件。若地址或认证配置不完整，直接说明缺少什么，不写入占位配置。
连接管理：{{AUTHORIZATION_GUIDE}}（仅供我管理或撤销，不是你接入的前置步骤）。

业务上请先做顾问：结合已有档案与材料，主动判断值得记录的重点、标签取舍和下一步，给出推荐及理由。观察先展示精简草稿供我审阅，标签增减/新建另列建议，等我确认后只执行同意部分。连接授权、修改草稿均不等于发布确认；已确认最终方案或我明确要求本次直接执行时，不重复确认。
