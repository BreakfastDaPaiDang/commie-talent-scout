请帮我连接“康米巨星猎头系统”的 MCP。

服务地址：{{MCP_SERVER_URL}}
认证配置：
{{AUTH_CONFIGURATION}}
凭据到期：{{CREDENTIAL_EXPIRES}}

这份专用凭据已由我在系统内创建并授权你用于连接，直接使用 HTTP Authorization 请求头即可，不需要浏览器，也不需要再去网页登录或申请凭据。按你所在客户端的格式配置以上信息，保护已有配置；不要在回复、日志或公开仓库中回显认证值，只保存到私有认证或配置位置。

先检查已有连接，并询问我是否需要持久配置，让以后新会话也能使用；如果我已经明确选择，按该选择继续。选择持久配置时，说明保存到哪个客户端、哪个范围，并验证连接设置和认证来源都能在新会话中生效，不能只设置当前进程。

连接后确认服务与成员身份，从 MCP 自带的工具说明和使用指南获取业务规则。最后说明当前是否连通、配置是否已保存，以及新会话是否验证过；不要把“已保存”当成“已验证”。若地址或认证配置不完整，说明缺少什么，不写入占位配置。
连接管理：{{AUTHORIZATION_GUIDE}}（仅供我管理或撤销，不是你接入的前置步骤）。
