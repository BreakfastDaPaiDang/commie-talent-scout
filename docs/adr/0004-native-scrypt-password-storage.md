# 使用原生 scrypt 存储密码

PBKDF2-SHA256 600,000 次在真实 Workers 环境超出运行时限制，因此选择已实测可运行、对应 OWASP 参数组合的原生 scrypt，而不降低 PBKDF2 迭代强度。

当前参数为 N=32768、r=8、p=3，密码记录携带算法版本与参数；云端探针 CPU 为 402 ms，正式部署需确认 Paid 档位并实现限速和 KDF 并发约束。
