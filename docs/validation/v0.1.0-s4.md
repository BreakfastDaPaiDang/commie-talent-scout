# v0.1.0 S4 验收

2026-09-08，对应 Issue #5。已部署人物/组织完整状态、按状态保存的成员绑定、关闭与显式重新开启。状态没有顺序限制；工作状态必须明确至少一名负责成员，不默认调用者。网页和 MCP 共用事务内版本、关闭及成员资格检查。

本地与 Cloudflare 各通过 4 组状态检查及 3 组成员绑定检查。并发关闭与资料编辑只有一项提交；关闭后管理员同样不能改资料或使用普通状态更新绕过重新开启。关闭、状态改变、重新开启分别留有明确事件，状态与成员原子生效；无变化不改版本或活动时间。

冻结后已有成员 ID 与负责人展示保留、带冻结标记；不能新分配冻结成员。关闭可携带当前既有关联，重新开启可保留原状态已有的冻结关联。目录按姓名检索、分页，供普通成员选择负责人，不暴露登录账号或密码管理字段。合成成员验收后已全部冻结。

网页完成缺少工作负责人时的提示和禁止保存、检索成员、选择负责人、关闭后隐藏编辑、显式重新开启；重开默认恢复关闭前状态与该状态原关联。实际 CSS 390 px 下，长状态“引荐中（待人事组接触）”、两名冻结成员、选择框及按钮均处于页面范围，表单纵向滚动；详情页面宽度 390，弹窗左右 21/369。截图限制延续 S2。

网络证据：首次云端状态检查有一次客户端 MCP 超时，服务记录显示修改已成功，用时 880 ms。随后查询原请求收据、以原 ID/参数重试，仍返回原版本且 replayed=true，没有第二次修改。另一轮测试启动遇到本机 IPv6 连接超时；验收脚本仅在自身进程优先 IPv4，随后完整云端 4 组检查通过。没有将连接失败误记为业务回滚，也没有修改站点网络或全局配置。

迁移 0005、本地类型检查与构建通过。脱敏结果见 [JSON](./v0.1.0-s4.json)。
