# 超期未更新提醒验收（Issue #33）

2026-09-09。

开启人物与组织以 30 天为一个月，人物引荐中（待人事组接触）、人事审核、已加入待对接使用 7 天。严格超过期限才报警，关闭与删除均排除。当前筛选结果内先展示超期档案，按最早到期排序；其他档案仍按最近更新排序。该排序在服务端分页前完成，续页固定首批判断时刻，避免只因翻页期间跨过期限而跳漏档案。

- 64 项业务测试、8 项 DOM 测试、类型检查与正式构建通过。
- 新增 4 项真实 SQLite 测试：7/30 天与超出 1 毫秒边界、全部三个工作状态、组织交流仍为 30 天、排序并分页、同到期时间、分页期间跨过阈值、无变化/草稿不延期、观察发布解除、关闭与重新开启、搜索/负责人/关闭/删除及范围计数。
- 新增 DOM 测试直接加载正式 `ArchivesPage`，验证已加载 35 条在周期重读后保留、报警与未读并存、真实编辑窗中未保存输入不被刷新打断、后台页面暂停。
- `check-ui.mjs` 四组通过：历史原型仍为不可变的 `531ab51`，四种宽度的普通卡片及其他布局无新差异；完整编辑、发布区、卡片和标签来源验证通过。
- `verify-archive-reminders.mjs` 本地及云端真实接口各 4 项通过：MCP 排序与分页、人物/组织阈值、桌面和手机标记不重叠无横向溢出、实际发布观察解除、手机完整状态编辑关闭解除。桌面和手机截图均已查看；云端截图与结果位于 [archive-reminders](archive-reminders/)。
- `verify-workspace-ui.mjs --remote` 6 项通过，包含完整资料与成员编辑、标签绑定与详情、历史版本、展开阅读、草稿及真实图片上传发布；浏览器无运行错误。

人工复核了服务端计算与展示的同一判断时刻、排序键与游标条件、过滤与未读数量、空保存和阅读不写入更新时间，以及定时列表重读不会重新挂载详情或清空草稿。无需数据库迁移，也不通过定时任务写入报警事件。

脚本只允许在隔离 staging 老化本次创建的虚构档案；收尾关闭并删除这些档案、冻结本次测试账号、撤销连接。未对生产业务记录调整时间，也未执行外部模型多轮测试。

测试站发布前备份：`snapshots/staging/2026-09-09T14-12-12.820Z-470860c7-31ec-45cd-8982-05f0a41ffaa5`；无新增迁移，现有 16 项兼容迁移验证通过。部署版本 `5141be0c-c2a2-4f63-b7b9-34e3209b09dd`，云端静态字节、鉴权及登录冒烟通过。
