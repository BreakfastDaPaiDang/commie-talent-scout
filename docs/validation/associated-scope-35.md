# 关联我验收（Issue #35）

2026-09-10。

旧实现将 `mine` 限制为开启档案的工作状态，导致普通观察、个人接触即使绑定本人也不命中。新增真实 SQLite 测试先复现失败：人物七种状态中仅三种工作状态计入，观察关联数量为零。移除工作/开启条件后，按当前状态成员绑定查询，列表与计数共用同一条件。

- 66 项业务测试、8 项 DOM 测试、类型检查、正式构建通过。
- 新增两项服务测试覆盖人物七状态、组织四状态、关闭筛选、搜索与成员组合、分页、计数、旧状态历史绑定排除、移除当前关联、创建者不自动关联及删除权限。
- `check-ui.mjs` 四组通过，继续使用不可变历史原型 `531ab51`，四种宽度无新布局差异，完整编辑、发布区、卡片与标签界面通过。
- `verify-associated-scope.mjs` 本地与云端真实接口各 4 项通过：MCP 普通/关闭关联和分页、HTTP 组织关联、桌面/手机共用“关联我”入口与正确卡片、解除关联后重新加载及旧 `mine` 偏好保留。截图已查看，手机无横向溢出，浏览器无运行错误。
- `verify-workspace-ui.mjs` 本地真实接口 6 项通过，覆盖完整资料、标签、状态与成员编辑、历史版本、草稿和图片发布。

原型从当前筛选集合统一计算全部、关联我、有未读三项数量；正式服务的 `scope=mine`、`counts.mine` 名称保留，已有浏览器偏好和 MCP 参数兼容。旧状态历史关联、创建者和观察作者不自动进入当前关联；关闭条件与其他筛选继续生效。无需数据迁移，不修改成员绑定或业务档案内容。

人工复核了查询条件、范围计数、当前状态与历史关联边界、服务指南及原型适配。脚本只使用本次创建的虚构档案，结束时关闭并删除，最后撤销测试连接。

隔离测试站发布前备份 `snapshots/staging/2026-09-09T16-53-42.951Z-725c1589-5772-4e03-98f0-1e1fbda2da7a`，部署版本 `9e51f78a-f25e-484a-bdea-b3edeaf92ebd`；现有 16 项迁移核对、静态资源字节、私有入口和登录冒烟通过。
