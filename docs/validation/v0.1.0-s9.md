# v0.1.0 S9 验收

2026-09-08，对应 Issue #3（依赖顺序中的第三项）。隔离测试环境已交付猎头账号创建、资料维护、临时密码重置、冻结/解冻、角色调整和管理历史，网页与 MCP 共享业务服务。

本地与 Cloudflare 各通过 5 组账号行为检查和 4 组真实 MCP 并发检查。创建重试返回同一账号，更换临时密码复用请求标识会冲突；首登须改密；重置使已有网页和 MCP 凭证立即失效；冻结阻止登录，解冻不复活旧凭证。另行复核收据的 replayed 标记，供调用分析区分重复尝试与新业务结果。

三个受控管理员同时通过 MCP 自我降权，恰好两项提交，一项返回 LAST_ADMIN_REQUIRED；始终保留一名未冻结管理员。旧连接下一次请求按新角色授权，降权成员无法自行恢复管理员。测试完成后原测试环境管理员已恢复，合成账号已冻结，保留账号 ID 与事件归属。冻结只修改账号状态及认证 epoch，不删除成员；档案作者和负责人展示将在 S4/S5 具备业务数据后补充联合验收。

网页实际完成创建、名称/QQ 保存、冻结、可读历史、最后管理员降权和冻结的拒绝反馈。临时密码由浏览器生成，创建/重置前要求用户保存；服务端不回显。敏感操作后重新读取当前身份，自己的重置/冻结进入重新登录，降权移出管理页；成员另有主动改密入口。390 px 实际 CSS 视口下页面宽度 390，管理弹窗左右界为 21/369，内容可纵向滚动。截图接口仍受 S2 记录的超时限制，未宣称本项完成截图视觉审查。

类型检查、构建及已有密码/秘密筛选测试通过。D1 迁移 0003 已应用本地和隔离云端；未创建生产数据库。脱敏检查结果见 [JSON](./v0.1.0-s9.json)。
