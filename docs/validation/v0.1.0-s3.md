# v0.1.0 S3 验收

2026-09-08，对应 Issue #4。正式档案列表、详情、创建与资料编辑已部署隔离测试环境，网页和 MCP 使用相同 Archives 命令及幂等收据。人物/组织类型不可修改，名称不强制唯一，必填名称与 QQ 字符串、联系方式备注、http/https 来源链接均有服务端校验。更新要求明确提供完整联系方式和链接，不因遗漏字段默默清空。

本地与 Cloudflare 各通过 4 组实际 HTTP/MCP 行为检查：网页创建后的 MCP 原请求重试不重复建档；无变化不改变活动时间、版本和历史；同版本并发修改只有一项提交；列表及事件分页不重复；同名对象允许分别创建，类型改变、空名称、缺少更新字段和危险链接被拒绝。历史包含操作者、时间、入口和字段前后值。所有数据均为虚构验收内容，生产尚未启用。

浏览器完成创建含 QQ/备注/来源链接的档案、复制联系方式、组织/人物切换、恢复搜索条件和选中档案，以及窄屏返回列表、编辑与查看变更历史。桌面 1920 px 下列表/详情为 672/1248 px 两栏；实际 CSS 390 px 下详情左右边界为 0/390，列表隐藏、返回可用，没有页面横向溢出。偏好以成员 ID 与实体类型为独立本地键，刷新保留；无存储权限时当前页面仍可用。截图接口限制延续 S2，不将 DOM 边界检查称作截图视觉验收。

列表上部使用独立状态及当前成员区域，底部固定显示观察条数与更新时间；人物与组织使用不同的原始默认形象，不采用姓名首字。当前观察数为 0，观察功能及负责人状态操作分别由 S5/S4 接续实现。没有将尚未交付的状态、观察与标签列为 MCP 能力。

类型检查、构建与迁移 0004 本地/云端应用通过。脱敏机器结果见 [JSON](./v0.1.0-s3.json)。
