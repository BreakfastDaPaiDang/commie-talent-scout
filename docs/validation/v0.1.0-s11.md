# v0.1.0 S11 验收

对应 Issue #7。标签与材料整理实现已部署隔离测试环境，生产尚未上线。**2026-09-09 已补齐云端浏览器联验**：独立 Edge 无界面进程打开此前真实 Codex 六轮验收留下的档案，核对完整正文、全部四个标签的类别与名称、具体依据及来源版本，桌面与 390px 手机视口通过。此轮没有重新运行六个 Codex 案例，也没有重写其业务产物。原先 `Debugger unattached` 的浏览器阻塞已由独立浏览器验证路径解除。

人物 34 项、组织 31 项初始词库通过迁移 0007/0008 初始化，分为各自的 7 个类别，不自动绑定档案。类别/标签稳定 ID、规范化唯一键、描述校验与定义历史由共享服务管理；并发创建不产生重复词条。网页支持按名称、类别、描述搜索，英文/中文冒号快捷拆分、就地新建后绑定、查看及维护依据、移除当前绑定和最多 3 个重点。公共定义维护、影响预览、筛选和合并由 S12/S14 继续交付。

本地与 Cloudflare 各通过 7 组行为检查，详见 [JSON](./v0.1.0-s11.json)：实际读过的观察版本才能作为 MCP 引用；跨库、无依据 Agent 绑定和关闭写入被拒绝；重复添加不覆盖依据，原请求重试不产生重复记录；一批多标签及重点更新只生成一个事件。关闭事务冻结当时定义/依据，重开返回差异并保留旧历史。隐藏唯一来源后，另一成员的标签、重点、事件、绑定数量和列表摘要一致隐藏；独立依据仍可见，编辑可见说明不会丢失隐藏引用，恢复来源不新增绑定。标签与关闭并发只允许一个提交。

列表由 SQL 先应用来源权限，再返回最多 3 个标签摘要及可见总数，不将全部依据复制到每个列表项。详情和历史按当前身份过滤引用说明；关闭快照同样遵守当前来源权限。词义、具体依据、原添加者和最近确认信息分别保存，历史不随公共定义更名而改写。

真实 Codex 使用隔离私有客户端、继承当前模型配置，每个案例独立冷启动，仅获得短任务和已接好的 MCP。最终 6 个案例全部通过：

- 代表性聊天：新增观察，保留视频剪辑、未提及的日语，按明确频道交接移除传播渠道，复用有时间范围的可用性；第一次运行新建音频修复，后续运行正确复用。
- 原样再次提供材料：业务版本及观察数量不变。
- 两个同名档案：询问识别信息，不猜测写入。
- 指代、发言者和转发时间不明的冲突材料：查旧依据，保留为待核实信息，不覆盖既有可用性标签。
- 一开一关两个对象：完成可写对象，准确报告关闭对象未完成，不擅自重开。
- 中途丢失响应：隔离测试转发器在真实服务已提交观察后故意丢弃一次成功响应并返回 503。Codex 查询原请求收据，继续完成标签；最终仍只有一条观察。此故障由测试注入，不表示正式服务发生了该故障。

人工对照原材料与实际保存结果：频道交接未被误判为技能消失，两支访谈片未被重复累计，字幕/粗剪与拍摄/配乐的职责边界保留；九月周三北京时间 20:00—22:00 与十月未知均保留；小敏的 Python 和小岚的英语没有归给阿禾，学习 Blender 不被当作已具备技能。首轮正文重复解释推断边界，已据此收紧服务内指南；复测将 335 字原聊天整理为 340 字分段观察，新增来源说明并保留关键事实，未靠机械字数评分判断质量。一次超出 schema 上限的查询参数被拒绝后，Codex 自行修正并继续。

本地真实浏览器已操作独立词库、搜索复用、手工无依据提示、查看来源版本、移除绑定、快捷创建和直接绑定；桌面字号与 390 CSS 像素布局检查通过，窄屏无横向溢出，标签详情正文 20 px、弹窗左右 21/369。当前 Chrome 使用 80% 页面缩放，验收视口按实际 CSS 宽度校准，完成后恢复视口。

独立网页 HTTP 入口核对真实 Codex 创建的观察正文、作者、标签依据、动态和受限列表摘要，与 MCP 实际产物一致。2026-09-09 扩展 `scripts/verify-compression-web.mjs --remote`，通过真实档案及观察深链接定位完整内容，逐项打开标签依据与引用版本；手机截图已人工查看，正文可阅读、标签保留完整类别，无横向溢出和运行异常。报告 [JSON](./v0.1.0-s11.json) 中 `web_joint` 记录新证据；与原 2026-09-08 的服务检查和 Codex 六轮产物分开保存。临时连接和网页会话均在收尾时撤销。

任务上下文由可选的 `record_task_context` 一次提交，区分用户原话与 Agent 摘要，未知原话保持未知，后续调用复用服务生成的 task ID。正文 30 天、元数据 180 天清理已接入；S13 继续完成任务审阅、统计和完整清理验收。测试凭证已撤销，复制的 Codex 登录文件已删除，测试配置中的 bearer 已清除；完整客户端日志留在本机私有目录，不进入仓库。

Cloudflare 上传期间发现本机代理 TLS 握手失败；同机直连 Cloudflare API 正常。相关验收命令仅临时直连并使用 IPv4 优先，不更改全局代理。最后部署版本：`722c5c72-631e-4ea4-ad5e-485861928a24`。类型检查及构建通过。
