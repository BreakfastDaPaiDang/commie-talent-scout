# 网页打开档案即已读（#36）

2026-09-10。需求：[Issue #36](https://github.com/BreakfastDaPaiDang/commie-talent-scout/issues/36)。决策：[ADR 0012](../adr/0012-mail-style-archive-reading.md)。

网页成功打开人物或组织档案后，一次确认打开时已有的可见未读。后续新增、源权限变化和自动刷新不会扩大原打开范围；MCP 仍使用逐事件内容票据，两者共享个人事件标记。原型同步打开行为，现有工作台布局不变。

## 验证

- 类型检查、17 项迁移清单校验、正式构建通过。76 项业务测试、17 项 DOM 测试全部通过。
- 新增服务测试覆盖完整个人快照、MCP 元数据无副作用、后来新增事件、重复请求、来源隐藏／恢复、关闭／删除／恢复、成员与连接隔离、过期凭据，以及确认事务前发生冻结、降权和删除。先复现再修复了撤销后仍确认、隐藏来源恢复后误确认、重复确认扩大范围三类问题。
- 新增 DOM 验证真实详情接入、一次打开不随自动刷新扩展、队列整档确认、跨档案导航、后台／隐藏／弹窗遮挡、失败重试、迟到详情取消、刷新同档案、旧成员响应、源权限例外、迟到导航与计数，以及长队列的剩余分页全属已读档案时自动完成。计数并发测试先复现旧请求把已清零角标恢复，再验证请求顺序保护。
- `check-ui.mjs` 四组回归通过：历史原型 `531ab51` 在 1920、1440、1024、390 四种宽度均无新几何差异；完整编辑、展开发布区、卡片及标签验收通过。
- `verify-reading-ui.mjs` 在本地和隔离云端各 7 项通过。真实 D1/HTTP/MCP 配合独立 Edge 浏览器，创建 37 条未读的虚构长档案，覆盖未加载分页的一次确认、读取失败、模拟后台可见性、丢失已成功确认响应后的原凭据重试、后来新增事件保留、过滤与计数、跨人物／关闭组织导航、删除观察上下文、同档案队列刷新，以及 MCP 单条确认与网页互通。
- 手机端实际完成资料链接编辑和观察发布，核对保存按钮完整可见、无横向溢出、观察只发布一次、本人发布不制造个人未读。已查看本地与云端完整编辑和发布截图。报告：[本地](./archive-reading/local.json)、[云端](./archive-reading/cloud.json)。
- `verify-workspace-ui.mjs` 本地和最终云端部署包的真实接口各 6 项通过：完整资料、标签、状态及成员、观察版本历史、私有草稿、真实 PNG 上传和图文发布。

## 部署与复核

迁移 `0017_archive_reading.sql` 只增加短期打开凭据及事件引用，保留旧事件阅读标记和 MCP 票据。确认与凭据处理标记在同一 D1 batch 提交，重复确认不补读首次提交时不可见的来源；删除／恢复触发器和到期清理连带移除临时引用。

首次隔离部署前备份 `snapshots/staging/2026-09-09T17-50-07.233Z-9a6e650f-f0dd-41ee-bc5e-19dc78162378`，部署版本 `7ef320ae-a853-4c86-bd92-7cd3135690c9`。17 项迁移、真实静态资源字节、私有入口及 D1 登录冒烟通过，随后完成云端阅读验收。导航计数的迟到响应保护另由专门 DOM 测试验证。

包含导航计数及长队列完成处理的最终隔离版本为 `8fd450b3-0c95-496f-8a37-00f97f0ffcb6`，发布前备份 `snapshots/staging/2026-09-09T18-01-29.474Z-5c7bd8ef-9dff-4e0d-a606-b9e990295bae`。最终包的迁移、静态资源和登录冒烟、云端工作台回归全部通过。

代码复核覆盖事务内鉴权、事件集合与上界、处理一次的确认语义、网页和 MCP 的入口差异、异步结果与成员隔离、草稿及选择保留。阅读脚本只创建虚构测试内容，完成后关闭并删除档案、冻结测试读者、撤销连接。既有 MCP 探针的未提交改动不属于本次交付。
