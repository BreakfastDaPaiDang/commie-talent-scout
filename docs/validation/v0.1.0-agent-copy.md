# 复制即接入验收（Issue #25）

2026-09-09，复制按钮自动为当前成员创建 90 天专用凭据，将实际 MCP URL、Authorization 和到期时间放入交接正文。重复复制复用本页内存中的有效凭据；另一个 Agent 创建独立连接，撤销后重新复制更换凭据。剪贴板拒绝时可选择同一份完整正文。原型与正式页面共用 `AgentHandoff`，没有将凭据写入浏览器持久存储。

- `verify-agent-copy.mjs` 本地和云端各 4 组通过：创建失败、双击、剪贴板失败重试、独立无浏览器客户端身份确认、另一连接、撤销后拒绝及重新创建。1440/390px 截图不包含凭据。
- `verify-codex-copied-onboarding.mjs` 云端 2 组通过：真实 Codex 首次启动没有预置 MCP，从网页复制正文保存到隔离客户端私有配置；真正退出并冷启动第二个 app-server，仅凭已保存配置发现 55 个工具，成功调用 `whoami`、`get_usage_guide`。服务端调用记录核对身份和专用连接，原有模型设置保留。临时认证及验收连接收尾撤销。
- 第一次真实 Codex 的 Python HTTP 自检曾被 Cloudflare 403/1010 拒绝，原失败保留；独立 urllib 默认 User-Agent 复现后，在两个系统域名的 `/mcp` 与 `/uploads/` 路径关闭 Browser Integrity Check，站点全局设置保留。`verify-browserless-http.mjs --remote` 验证有效 MCP 200、未认证 401、上传缺票据 401、有效 Python PUT 200、发布图片及撤销后 401，共 3 组通过。
- 类型检查、44 项服务测试、5 项 DOM 测试、构建、历史四宽度对照、完整编辑与发布区、卡片和注意力界面验证通过。旧版接入说明已同步更新，连接后不会再次要求无浏览器 Agent 到网页申请凭据。

本报告不替代 Issue #11/#13 的词库真实 Codex 写入，以及 Issue #14 的正式环境完整验收与成员网络报告。
