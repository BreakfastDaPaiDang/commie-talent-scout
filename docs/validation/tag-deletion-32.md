# 词库删除与默认查询验收（Issue #32）

2026-09-09。

原问题以真实 SQLite 业务服务复现：停用一个虚构类别后，`Tags.categories('person')` 仍返回它，默认发现断言失败。修复后类别和词条均默认只返回可用项；停用与删除分别显式查询。

- 服务回归：全套原 59 项通过；随后新增任务删除产物回溯测试通过（当前总计 60 项）。删除专项 4 项包含默认过滤、名称检索/释放/恢复冲突、类别下词条、关闭引用、管理员权限、冻结、并发及事务内竞争。
- DOM 7 项通过；类型检查、正式构建通过。
- `check-ui.mjs` 四组通过：继续对照不可变历史原型 `531ab51`，四个宽度无新几何差异；完整编辑、发布区、卡片和标签来源通过。
- `verify-workspace-ui.mjs` 本地真实接口 6 项通过，覆盖完整编辑、标签绑定、资料/状态及图文发布。
- `verify-tag-deletion.mjs` 本地和云端各 4 项通过：MCP 默认过滤、桌面实际删除及提交响应丢失重试、手机已删除检索/恢复、手机类别删除。原引用保留，恢复仍为停用，单次提交只产生一个定义版本。
- `verify-tag-maintenance.mjs --remote` 原有维护 4 项通过：真实影响预览、并发依据变化拒绝、历史定义、关闭快照与手机完整表单。
- 云端验收均清理本次实际新建的类别和词条。清理使用本次测试连接的认证，不依赖已经退出的测试浏览器会话，并最终撤销连接。

测试站发布前备份：`snapshots/staging/2026-09-09T13-29-53.586Z-f453bce2-7072-481e-ac58-26aab6e3657c`。兼容迁移 0016、云端静态字节及登录冒烟通过；版本 `07ce07ce-2b71-49f7-a6b6-957f377538c0`。

截图由真实接口脚本生成至 `tmp/verification/tag-deletion/`，已逐张查看桌面删除、手机恢复和手机类别删除的完整确认态；按钮可达、无横向溢出。没有启动真实外部模型的多轮 Agent 测试。

删除是可恢复的业务词库移除，明确 ID 的历史引用仍可追溯，不宣称彻底清除历史或备份。
