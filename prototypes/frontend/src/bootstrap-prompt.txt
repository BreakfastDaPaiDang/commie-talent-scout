请帮我连接“康米巨星猎头系统”的 MCP。

服务地址：{{MCP_SERVER_URL}}
认证入口：{{AUTHORIZATION_GUIDE}}

先检查已有连接，并询问我是否需要持久配置，让以后新会话也能使用；如果我已经明确选择，按该选择继续。选择持久配置时，说明保存到哪个客户端、哪个范围，并验证连接设置和认证来源都能在新会话中生效，不能只设置当前进程。

保护现有配置，不在聊天中索要或回显密码、令牌。没有有效地址或认证入口时，说明缺少什么，不写入占位配置。连接后确认服务与成员身份，从 MCP 自带的工具说明和使用指南获取业务规则。最后说明当前是否连通、配置是否已保存，以及新会话是否验证过；不要把“已保存”当成“已验证”。
