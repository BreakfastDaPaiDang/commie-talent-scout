export const protocolText = `康米巨星猎头系统使用说明（原型说明，地址与凭证待正式服务提供）
先确认当前成员身份，再检索已有档案。状态没有顺序；状态与绑定成员是两项独立信息。
人物的引荐中、人事审核、已加入待对接，以及组织的组织交流，都是工作状态，必须明确绑定负责成员。不要擅自把调用者指定为负责人；信息不足时向用户询问。
其他状态也可关联成员作记录，但不强制。新建和整理资料时，主动检查材料里是否有联系方式、QQ、关联成员、来源链接或图片；有明确材料就填写，有用但缺失的信息用一句有针对性的追问或完成后提醒，不要因为非必填就永久忽略，也不要为每个空字段逐条盘问。
人物 QQ 自动获取头像，组织使用上传头像；档案不设置别名或简介。
关闭后须重新开启才能修改；仅作者或管理员可编辑、删除和恢复记录。已删除内容、旧版本和图片仅原作者/管理员可读，不能绕过。
阅读实际返回的内容后自动确认阅读进度，不要求用户额外标为已读。
图片须先上传再引用附件标识，客户端需要本机文件读取与 HTTP 上传能力。写入携带请求 ID 和预期版本，失败重试不得重复创建。`;
